{
  "compilerOptions": {
    "target": "es6", // Chọn phiên bản ECMAScript
    "module": "commonjs", // Loại module (có thể thay đổi nếu cần)
    "baseUrl": "src", // Thiết lập thư mục gốc cho các đường dẫn tuyệt đối
    "paths": {
      "@components/*": ["components/*"], // Đường dẫn đến thư mục components
      "@action/*": ["action/*"], // Đường dẫn đến thư mục action
      "@hooks/*": ["hooks/*"], // Đường dẫn đến thư mục hooks (nếu có)
      "@utils/*": ["utils/*"], // Đường dẫn đến thư mục utils (nếu có)
      "@styles/*": ["styles/*"], // Đường dẫn đến thư mục styles (nếu có)
      "@pages/*": ["pages/*"] // Đường dẫn đến thư mục pages (nếu có)
    }
  }
}
